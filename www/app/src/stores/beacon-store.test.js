var BeaconStore = require('./beacon-store');

describe('BeaconStore::getClosestBeacon()', function () {

  // mock window.EstimoteBeacons
  //window.EstimoteBeacons = {
    //startRangingBeaconsInRegion: function () {
      //console.log('asfs');
    //}
  //};

  //beforeEach(function () {
  //});

  //it('should return an object', function () {
    //var beacon = BeaconStore.getClosestBeacon();
    //expect(typeof beacon).toBe('object');
  //});

  //it('should have an id property', function () {
    //expect(this.beacon.id).toBeDefined();
  //});

  //it('should have an distance property', function () {
    //expect(this.beacon.distance).toBeDefined();
  //});

});
